package org.java.core.base.Enum;

/**
 * ThreadStates is the enum with fixed constants 
 * fields START, RUNNING, WAITING and DEAD.
 * ThreadStates是具有固定常量字段START，RUNNING，WAITING和DEAD的枚举。
 */
public enum ThreadStates {
	START,
	RUNNING,
	WAITING,
	DEAD;
}
