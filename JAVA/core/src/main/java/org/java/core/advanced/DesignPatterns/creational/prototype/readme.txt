Prototype Pattern（原型模式）：

什么时候使用原型模型?
	当对象创建成本高昂并且需要大量时间和资源并且您已经存在类似对象时，使用原型设计模式。

 因此，此模式提供了一种机制，可将原始对象复制到新对象，
 然后根据需要对其进行修改。 此模式使用Java克隆来复制对象。

原型设计模式要求您复制的对象应提供复制功能。 它不应该由任何其他类完成。
但是，是否使用对象属性的  shallow or deep copy 取决于要求，这是一个设计决策。

