<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" 
	xmlns:p="http://www.springframework.org/schema/p"
	xmlns:context="http://www.springframework.org/schema/context" 
	xmlns:tx="http://www.springframework.org/schema/tx" 
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xsi:schemaLocation="
		http://www.springframework.org/schema/beans
		http://www.springframework.org/schema/beans/spring-beans-3.0.xsd
		http://www.springframework.org/schema/context
		http://www.springframework.org/schema/context/spring-context-3.0.xsd
		http://www.springframework.org/schema/tx
		http://www.springframework.org/schema/tx/spring-tx.xsd
		">
	<!-- spring bean configuration file (Spring Config)-->
	
	<!--
		1. 首先我们告诉spring我们要对Spring组件（Services，DAOs）使用类路径扫描，
		而不是在spring xml中逐个定义它们。我们还启用了Spring注释检测。
		2. 添加数据源，即当前HSQLDB内存数据库。
		3. 我们设置了一个JPA EntityManagerFactory，它将由应用程序使用来获取EntityManager。
		Spring支持3种不同的方法来做这个，我们使用LocalContainerEntityManagerFactoryBean来实现完整的JPA功能。
		
		我们将LocalContainerEntityManagerFactoryBean属性设置为：
		
			a. packagesToScan属性指向我们的model classes包。
			b. 在spring配置文件中提前定义的datasource。
			c. jpaVendorAdapter作为Hibernate并设置了一些hibernate属性。
			
		4. 我们将创建Spring PlatformTransactionManager实例做为JpaTransactionManager。
		此事务管理器适用于使用单个JPA EntityManagerFactory进行事务数据访问的应用程序。
		5. 我们“基于注释”启用事务行为的配置，并设置我们创建的transactionManager。
	-->
	
	<!-- Scans the classpath for annotated components that will be auto-registered as Spring beans -->
	<context:component-scan base-package="hu.daniel.hari.learn.spring" />
	<!-- Activates various annotations to be detected in bean classes e.g: @Autowired -->
	<context:annotation-config />

	<bean id="dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
		<property name="driverClassName" value="org.hsqldb.jdbcDriver" />
		<property name="url" value="jdbc:hsqldb:mem://productDb" />
		<property name="username" value="sa" />
		<property name="password" value="" />
	</bean>
	
	<bean id="entityManagerFactory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean"
			p:packagesToScan="hu.daniel.hari.learn.spring.orm.model"
            p:dataSource-ref="dataSource"
			>
		<property name="jpaVendorAdapter">
			<bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter">
				<property name="generateDdl" value="true" />
				<property name="showSql" value="true" />
			</bean>
		</property>
	</bean>

	<!-- Transactions -->
	<bean id="transactionManager" class="org.springframework.orm.jpa.JpaTransactionManager">
		<property name="entityManagerFactory" ref="entityManagerFactory" />
	</bean>
	<tx:annotation-driven transaction-manager="transactionManager" />

</beans>