Java总是值传递

 just remember that variables are references or pointers 
 and it’s copy is passed to the methods, so java is always pass by value
 记得变量是引用或指针，它的副本传递给方法(或者理解为只是把它的值传递复制给方法参数而已)，因此java总是按值传递